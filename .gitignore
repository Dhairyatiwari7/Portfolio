# Dependencies (auto-created by npm install — do not commit)
node_modules/

# Build output (auto-created by npm run build — do not commit)
dist/

# Env secrets
.env
.env.local
.env.*.local

# OS / logs
.DS_Store
*.log
